# Client Configuration (Development only)
# In production, the backend serves the React build from the same domain

# For local development only:
# REACT_APP_API_URL=http://localhost:5000

# This project uses relative API paths (/api/...) for seamless deployment
# The proxy in package.json handles localhost:5000 during development
